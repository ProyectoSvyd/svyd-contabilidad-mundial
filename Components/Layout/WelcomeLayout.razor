@inherits LayoutComponentBase

<div class="page">
    <link href="css/welcome.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
    
    <div id="blazor-error-ui">
        <div class="blazor-error-container">
            <div class="blazor-error-content">
                <h3>🚨 Oops! Algo salió mal</h3>
                <p>Ha ocurrido un error inesperado. Nuestro equipo ha sido notificado.</p>
                <div class="blazor-error-actions">
                    <a href="" class="reload-button">🔄 Recargar página</a>
                    <button class="dismiss-button" onclick="document.getElementById('blazor-error-ui').style.display='none'">❌ Cerrar</button>
                </div>
            </div>
        </div>
    </div>

    @Body
    
    <script src="js/welcome-effects.js"></script>
</div>

<style>
    /* Estilos para el mensaje de error personalizado estilo SVYD */
    #blazor-error-ui {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.8);
        z-index: 10000;
        display: flex;
        align-items: center;
        justify-content: center;
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
    }
    
    .blazor-error-container {
        background: rgba(255, 255, 255, 0.95);
        border-radius: 20px;
        padding: 2rem;
        max-width: 400px;
        text-align: center;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        border: 1px solid rgba(255, 255, 255, 0.3);
        backdrop-filter: blur(30px);
        -webkit-backdrop-filter: blur(30px);
    }
    
    .blazor-error-content h3 {
        color: #dc2626;
        margin-bottom: 1rem;
        font-family: 'Inter', sans-serif;
        font-weight: 600;
    }
    
    .blazor-error-content p {
        color: #374151;
        margin-bottom: 1.5rem;
        font-family: 'Inter', sans-serif;
        line-height: 1.5;
    }
    
    .blazor-error-actions {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
    }
    
    .reload-button, .dismiss-button {
        padding: 0.75rem 1.5rem;
        border: none;
        border-radius: 12px;
        font-family: 'Inter', sans-serif;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.3s ease;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    .reload-button {
        background: linear-gradient(135deg, #22c55e, #16a34a);
        color: white;
    }
    
    .reload-button:hover {
        background: linear-gradient(135deg, #16a34a, #15803d);
        transform: translateY(-1px);
        color: white;
        text-decoration: none;
    }
    
    .dismiss-button {
        background: rgba(107, 114, 128, 0.1);
        color: #6b7280;
        border: 1px solid rgba(107, 114, 128, 0.3);
    }
    
    .dismiss-button:hover {
        background: rgba(107, 114, 128, 0.2);
        transform: translateY(-1px);
    }
</style>